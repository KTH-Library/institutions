# Config used internally

This config holds the remote GRID dataset download url and the
corresponding local destination on disk

## Usage

``` r
institutions_cfg()
```

## Value

list with two slots, one with the base_url of the dataset one with the
local download locations
