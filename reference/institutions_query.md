# Run a custom SQL query

Run a custom SQL query

## Usage

``` r
institutions_query(sql_query, ...)
```

## Arguments

- sql_query:

  the query

- ...:

  other arguments to be passed to the sql() fcn
